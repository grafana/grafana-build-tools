#!/bin/sh

set -e
set -u

git clone --depth 1 --branch "${1}" https://github.com/keilerkonzept/dockerfile-json dockerfile-json
cd dockerfile-json
env GOPATH=/build GOOS="${2}" GOARCH="${3}" CGO_ENABLED=0 go install -tags=dfrunsecurity .
